describe EnquiriesController, type: :controller do

  # before(:each) do
  #   @enquiry = create(:enquiry)
  #   @activity = @enquiry.activity
  # end

  # context "GET new" do

  #   it "assigns a blank enquiry to the view" do
  #     # get :new, { activity_id: @activity.id }
  #     expect(assigns(:enquiry)).to be_a_new(Enquiry)
  #     # expect(response).to render_template(partial: 'new')
  #   end
  # end
end
