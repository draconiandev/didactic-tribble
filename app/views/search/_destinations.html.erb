<% if @destinations.any? %>
  <h4 class="center">Destinations</h4>
  <h5><%= @destinations.count %> results found</h5> 
  
  <% @destinations.each do |destination| %>
    <p><%=link_to destination.name, destination_path(destination) %></p>
  <% end %>

<% end %>
