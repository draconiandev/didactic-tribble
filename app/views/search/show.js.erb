$('[data-behavior="endless-scroll"]').append('<%= j render @activities %>');

ReactRailsUJS.mountComponents();

<% if @activities.next_page %>
  $('.pagination').replaceWith('<%= j will_paginate @activities %>');
<% else %>
  $('.pagination').remove();
<% end %>