<%= render 'shared/header' %>
<div class="container">
  <div class="center-align">
    <h4>Outstanding Experiences</h4>
    <h5>You have to do it. At least, once.</h5>
  </div>
  <%= render 'shared/activities' %>
</div>
<%= render 'shared/destinations' %>
<%= render 'shared/handcrafted' %>
